'use strict';

/**
 * consultation-consulting-room service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::consultation-consulting-room.consultation-consulting-room');
