'use strict';

/**
 * consultation-consulting-room router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::consultation-consulting-room.consultation-consulting-room');
