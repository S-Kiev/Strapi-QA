'use strict';

/**
 * consultation-consulting-room controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::consultation-consulting-room.consultation-consulting-room');
